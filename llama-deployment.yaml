apiVersion: apps/v1
kind: Deployment
metadata:
  name: llama-server
  namespace: default
spec:
  replicas: 2            # HPA can scale this up later
  selector:
    matchLabels:
      app: llama-server
  template:
    metadata:
      labels:
        app: llama-server
    spec:
      containers:
      - name: llama
        image: ghcr.io/ggml-org/llama.cpp:server-cuda
	# use llama.cpp:server if you dont have GPU
        # 'server' is the entrypoint; these are arguments to it
        args:
          - -m
          - /models/mistral-7b-instruct.Q4_K_M.gguf
          - --n-gpu-layers
          - "999"
          - -c
          - "2048"
          - --host
          - 0.0.0.0
          - --port
          - "8080"
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            cpu: "2"
            memory: "4Gi"
            nvidia.com/gpu.shared: 1
          limits:
            cpu: "4"
            memory: "8Gi"
            nvidia.com/gpu.shared: 1
        volumeMounts:
        - name: models
          mountPath: /models
      volumes:
      - name: models
        persistentVolumeClaim:
          claimName: models-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: llama-service
  namespace: default
spec:
  selector:
    app: llama-server
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP

